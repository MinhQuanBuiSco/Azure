# Local development (same as Blog 3 — setup.sh writes these)
AZURE_TENANT_ID=your-tenant-id
AZURE_API_CLIENT_ID=your-api-client-id

# In Azure, the container has AZURE_KEY_VAULT_URL set instead,
# and reads TENANT_ID + API_CLIENT_ID from Key Vault via Managed Identity.
